# Assets

Put your brand assets (logo, images, etc.) in this folder.

## Logo
- Replace `logo-placeholder.svg` with your real logo, or add a new file (for example `logo.png`).
- Then update imports in:
	- `src/components/Header.tsx`
	- `src/components/Footer.tsx`
